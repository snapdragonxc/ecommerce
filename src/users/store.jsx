
export default {
  isAuthenticated: false,

/*  get: () => ({
  //  getElementById username = localStorage.getItem('username');

    if (username) {
      username = JSON.parse(username);
    }
    return username;
  },

  set: (username) => localStorage.setItem('username', JSON.stringify(username)),

  remove: () => localStorage.removeItem('username') */
};
